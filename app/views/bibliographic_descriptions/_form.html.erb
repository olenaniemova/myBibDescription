<%= form_with(model: bibliographic_description, local: true) do |form| %>
  <% if bibliographic_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bibliographic_description.errors.count, "error") %> prohibited this bibliographic_description from being saved:</h2>

      <ul>
        <% bibliographic_description.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.text_field :profile_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
