<%= form_with(model: bibliographic_style, local: true) do |form| %>
  <% if bibliographic_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bibliographic_style.errors.count, "error") %> prohibited this bibliographic_style from being saved:</h2>

      <ul>
        <% bibliographic_style.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
